version: '3'
services: 
    bot:
        build: .
        ports:
            - "22:22"
        volumes:
            - .:/bot
    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_PASSWORD: linnanvahti
            POSTGRES_DB: ovivahtidb
    dbadmin:
        image: adminer
        restart: always
        volumes:
            - /OvivahtiDB:/var/lib/postgresql/data
        ports:
            - 8080:8080
